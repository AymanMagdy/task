This is repositiory is to complete a task for the UNIX course on Coursera. The repo is basically to run the guessinggame.sh and it will ask the user to enter the number of files in the current directory. If the user entered the number correctly the program will congrates the user and exit. if the entered number is larger than expected, the program will tell that to the user and will keep running. If the entered number is smaller than expected, the program will tell that to the user and the program will keep running.
